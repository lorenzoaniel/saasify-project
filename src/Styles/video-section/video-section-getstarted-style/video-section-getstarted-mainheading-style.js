import { css } from "styled-components";

export const videoSectionGetstartedMainheadingStyle = css`
	width: 60%;
	/* height: 100px; */

	font-family: var(--DefaultFont-inter);
	font-weight: 700;
	font-size: clamp(4vw, 48px, 24px);
	line-height: 100%;

	/* or 48px */
	letter-spacing: -0.03em;

	color: #ffffff;
`;
